var React = require('react');

var Todo = React.createClass({
    render() {
      return (
        <nav id="navigation" className="navigation">
          <a href="#">x</a>
        </nav>
      );
    }
});

module.exports = Todo;
