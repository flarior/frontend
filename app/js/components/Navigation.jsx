var React = require('react');

var Menu = React.createClass({
    render() {
      return (
        <nav id="navigation" className="navigation">
          <a href="#">x</a>
        </nav>
      );
    }
});

module.exports = Menu;
